export const state = () => ({
    // visibility: false,
})
export const mutations = {
    // sets the attribute visibility
    changeVisible(state) {
        state.visibility = !state.visibility
    },
}
